#include "wifi.h"
#include "includes.h"

const char* testWifiConnection(WIFI_ERROR_MESSAGE_t errorcode);
const char* testTcpConnection(WIFI_ERROR_MESSAGE_t errorcode);
const uint8_t* callbackTest(uint8_t* receiveParameter);